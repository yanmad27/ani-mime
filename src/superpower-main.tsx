import React from "react";
import ReactDOM from "react-dom/client";
import { SuperpowerTool } from "./components/SuperpowerTool";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <SuperpowerTool />
  </React.StrictMode>,
);
